# Inn room assets

- `inn-room-v0.1.webp`: text-free illustrated background for the boy's first room, viewed from the doorway.
- `../../src/inn-room-hotspots.svg`: separate SVG interaction layer aligned to the room background.

Keep visible vocabulary labels out of the raster background. Interaction labels and states should remain in HTML/SVG so they can change without regenerating the illustration.
