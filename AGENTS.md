# AGENTS.md

이 저장소는 번체 중국어 단어 전술게임 `chinese-word-tactics`의 독립 개발 저장소다.

## 제품 목표

- 모바일 세로 화면, 특히 iPhone 13 mini Safari에서 자연스럽게 플레이할 수 있어야 한다.
- 단어의 뜻을 이용해 플레이어가 직접 해법을 찾는 경험을 우선한다.
- 단어 조회는 페널티 없이 허용한다.
- 시간 제한, 별점, 에너지, 반복 파밍을 핵심 루프에 넣지 않는다.
- 추천 경로와 실제 잠금 조건을 구분한다.

## 구현 원칙

- 기존 튜토리얼 v0.3의 동작을 기준선으로 보존한다.
- 이전 과정에서 새 기능을 섞지 않는다. 먼저 같은 동작을 독립 저장소에서 재현한다.
- 정적 GitHub Pages 환경에서 동작해야 한다.
- 프로젝트 경로 `/chinese-word-tactics/`에서도 깨지지 않도록 절대 루트 경로를 피한다.
- 새 프레임워크를 도입하지 않는다. HTML/CSS/Vanilla JS/JSON을 기본으로 유지한다.
- 단어별 전용 엔진 함수보다 거리, 영역, 상태 변화, 자원 이동 같은 공통 판정을 우선한다.
- 스테이지 데이터는 가능한 한 코드와 분리하되, 모든 규칙을 JSON으로 억지로 옮기지 않는다.

## 데이터 원칙

- TBCL 등 공식 원자료와 게임 편집층을 분리한다.
- 공식 발음·등급·출처 정보는 게임 설계 때문에 수정하지 않는다.
- 한국어 뜻, 기믹 태그, 마을 배치, 대비어 등은 게임 편집층에서 관리한다.
- 한 스테이지의 새 목표어는 기본적으로 1~3개를 권장한다.
- 같은 단어의 재등장을 적극적으로 사용한다.

## 변경 방식

- 현재는 1.0 이전이므로 기능을 장기 작업 브랜치에 묵히기보다 `main`에 작게 완결된 단위로 빠르게 반영한다.
- `main` 반영 → GitHub Pages 배포 → iPhone 13 mini Safari 실기기 확인 → 즉시 수정의 짧은 주기를 우선한다.
- 요청 범위를 넘어선 리팩터링을 피한다.
- 기존 실험판을 삭제하거나 덮어쓰지 않는다.
- 모바일 UI는 최소 44px 터치 영역과 safe-area를 고려한다.
- 같은 의미의 규칙이 두 번째 실제 사용처에 등장하면 필요한 범위까지만 공통 기믹으로 일반화한다. 너무 이른 범용 엔진화는 피한다.

## 검증

변경 후 최소한 다음 흐름을 확인한다.

1. 모든 튜토리얼 스테이지 진입
2. 이동/위험 회피/기다리기/살펴보기/되돌리기
3. 단어 카드 열기와 닫기
4. 완료 상태와 로컬 저장
5. iPhone 크기의 세로 뷰포트에서 화면 잘림 여부
6. GitHub Pages 하위 경로에서 정적 자원 로딩
7. 신규 본편 스테이지의 첫 플레이와 재플레이가 본편 저장을 섞지 않는지 확인

현재 기준선은 `legacy/tutorial-v03.html`과 루트 `index.html` 조합이다. 길목 G1 `進入 / 退出`은 iPhone 13 mini Safari 실기기에서 판정과 상황형 길표지 조사를 확인했다.
